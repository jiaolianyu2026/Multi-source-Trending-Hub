# 今日热搜 · 产品需求文档 (PRD)

**版本**: v1.0  
**日期**: 2026-05-28  
**状态**: 待评审  

---

## 1. 产品概述

### 1.1 产品定位

**今日热搜** 是一个多平台热搜聚合网站，支持大模型智能数据分析。

**核心价值**：帮助用户一站式快速了解全网热点动态，减少多平台切换成本，同时借助大模型能力对感兴趣的热点进行深度解读和个性化分析。

### 1.2 核心功能

首页以**标签页形式**展示两大板块：

1. **平台热搜**：展示知乎、B站、微博3个卡片网格，实时聚合各平台热门内容
2. **数据分析**：支持用户自定义主题，基于聚合热帖内容，通过与大模型对话方式进行个性化分析

### 1.3 项目性质

**技术学习项目**，非商业用途。所有数据来源于公开渠道，仅供个人学习研究使用。

---

## 2. 目标用户

### 2.1 用户画像

| 用户类型 | 描述 | 使用目的 |
|---------|------|---------|
| **信息获取型用户** | 上班族、学生、内容创作者 | 快速了解全网热点动态，追踪感兴趣的话题 |
| **技术学习者** | 对Vibe Coding/全栈开发感兴趣的学习者 | 参考项目实现，学习前后端开发、数据聚合、LLM集成等技术 |

### 2.2 典型使用场景

- **晨间浏览**：每日开启平台热搜标签页，快速扫读知乎、B站、微博三平台热点
- **热点解读**：针对某条热搜内容，使用数据分析功能进行主题化解读（如"分析这条热搜的传播特点"）
- **趋势洞察**：基于聚合数据，通过对话方式探索特定领域的近期趋势

---

## 3. 核心功能

### 3.1 MVP（必须实现）- P0

#### 3.1.1 首页标签页布局

| 功能项 | 说明 | 优先级 |
|-------|------|-------|
| 标签页切换 | 首页采用标签页分区设计，包含「平台热搜」「数据分析」两大独立板块 | P0 |
| 响应式布局 | 桌面多列、手机单列自适应 | P0 |

#### 3.1.2 平台热搜板块

| 功能项 | 说明 | 优先级 |
|-------|------|-------|
| 三平台聚合 | 知乎热榜、B站热搜、微博热搜 | P0 |
| 卡片网格展示 | 每平台一个卡片，内部展示Top 50榜单 | P0 |
| 数据字段 | 排名(rank)、标题(title)、热度值(heat)、跳转链接(url) | P0 |
| 更新时间显示 | 卡片展示「更新于 x 分钟前」 | P0 |
| 强制跳转 | 点击条目跳转至原平台查看完整内容，不做内容镜像 | P0 |

#### 3.1.3 数据分析板块

| 功能项 | 说明 | 优先级 |
|-------|------|-------|
| 对话界面 | 类似ChatGPT的对话式交互界面 | P0 |
| 数据源范围 | 基于3平台共150条热搜数据的聚合内容 | P0 |
| 主题自定义 | 支持用户输入任意分析主题 | P0 |
| 流式输出 | 大模型响应采用流式输出，提升用户体验 | P0 |
| 筛选能力 | 支持指定特定平台、关键词筛选分析范围 | P0 |

#### 3.1.4 数据接口与缓存

| 功能项 | 说明 | 优先级 |
|-------|------|-------|
| 后端API | 自建后端中转请求，解决跨域问题 | P0 |
| 统一缓存 | 缓存周期10分钟，Redis或本地内存 | P0 |
| 降级方案 | 接口异常时返回缓存数据（即使已过期），同时触发异步更新 | P0 |
| 异常处理 | 单平台失败不影响其他卡片，显示友好提示 | P0 |

#### 3.1.5 部署与合规

| 功能项 | 说明 | 优先级 |
|-------|------|-------|
| HTTPS部署 | 支持HTTPS协议，公网可访问 | P0 |
| 非商用声明 | 页脚明确标注技术学习项目、非商业用途 | P0 |
| 免责声明 | 数据来源声明、版权归属声明 | P0 |
| robots.txt | 禁止搜索引擎收录 | P0 |
| 访问限速 | 单用户每分钟访问次数限制 | P0 |

### 3.2 后续规划（P1）

| 功能项 | 说明 | 优先级 |
|-------|------|-------|
| 博主热点聚合 | 支持用户通过「博主ID+所属平台」关注指定博主 | P1 |
| 更多平台 | 百度热搜、头条热搜等额外平台接入 | P1 |
| 历史榜单 | 查看历史热搜数据 | P1 |
| 用户系统 | 登录、个人中心、分析记录保存 | P1 |
| 暗色模式 | 主题切换功能 | P1 |

> **说明**：当前已实现平台为知乎、B站、微博三平台。百度、头条等为规划中待接入平台。

---

## 4. 功能详细设计

### 4.1 平台热搜板块

#### 4.1.1 页面布局

**整体结构：**

```
┌────────────────────────────────────────────────────────────┐
│  [🔴] 今日热搜          实时聚合全网热点话题      10:30 更新  │  ← Header (56px)
├────────────────────────────────────────────────────────────┤
│  [平台热搜] [数据分析]                                      │  ← Tabs
├────────────────────────────────────────────────────────────┤
│                                                            │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│   │ [知] 知乎    │  │ [B] B站      │  │ [微] 微博    │    │  ← 3 Cards
│   │    热榜 今日 │  │  热搜榜 UP主 │  │ 热搜榜 实时  │    │
│   ├──────────────┤  ├──────────────┤  ├──────────────┤    │
│   │ 🔴1 标题     │  │ 🟠1 标题     │  │ 🟡1 标题     │    │
│   │      热度 AI │  │      播放 AI │  │      阅读 热 │    │
│   │ 🟠2 标题     │  │ 🟡2 标题     │  │  2 标题      │    │
│   │      热度 金融│  │      播放 国际│  │      阅读 AI │    │
│   │ 🟡3 标题     │  │  3 标题      │  │  3 标题      │    │
│   │ ...          │  │ ...          │  │ ...          │    │
│   │ 50 标题 热度 │  │ 50 标题 播放 │  │ 50 标题 阅读 │    │
│   ├──────────────┤  ├──────────────┤  ├──────────────┤    │
│   │ 更新于：刚刚  │  │ 更新于：刚刚  │  │ 更新于：刚刚  │    │  ← Footer
│   └──────────────┘  └──────────────┘  └──────────────┘    │
│                                                            │
└────────────────────────────────────────────────────────────┘
                         [页脚声明]
```

#### 4.1.2 数据结构

**统一热搜条目格式：**

```typescript
interface HotSearchItem {
  rank: number;        // 排名
  title: string;       // 标题
  url: string;         // 跳转链接
  heat?: number;       // 热度值（可选）
  platform: 'zhihu' | 'bilibili' | 'weibo';
  extra?: {
    answerCount?: number;   // 知乎：回答数
    viewCount?: number;     // 知乎：浏览数
    playCount?: string;     // B站：播放量
    readCount?: string;     // 微博：阅读数
    discussCount?: string;  // 微博：讨论数
  };
  tag?: {                   // 内容标签（可选）
    name: string;           // 标签名，如"AI"、"金融"、"国际"
    color: string;          // 标签颜色，如"#8b5cf6"、"#f59e0b"
  };
}
```

#### 4.1.3 数据源技术方案

| 平台 | 技术方案 | 数据内容 | 频率控制 |
|-----|---------|---------|---------|
| **知乎** | zhihu-hot-api 开源API | 热榜标题、排名、热度值、问题链接、回答数、浏览数 | 缓存10分钟，≤20次/小时 |
| **B站** | bilibili-API-collect 热搜接口 | 视频标题、排名、视频链接、播放量、热度标签 | 缓存10分钟，≤30次/小时 |
| **微博** | 开源API方案（weibo-hot-search） | 热搜标题、排名、热度值、话题链接、讨论数、阅读数、标签 | 缓存10分钟，≤30次/小时 |

#### 4.1.4 缓存策略

```
缓存键格式：hotsearch:{platform}:{date}
缓存介质：Redis 或 本地内存
过期时间：10分钟
降级方案：接口异常时返回缓存数据（即使已过期），同时触发异步更新
```

### 4.2 数据分析板块

#### 4.2.1 数据分析卡片布局

**单张分析卡片结构（高度520px）：**

```
┌─────────────────────────────────┐
│ [析] A股市场分析          [×]   │  ← Header (图标+标题+删除)
│         数据分析 · GPT-4        │
├─────────────────────────────────┤
│                                 │
│  模型 [GPT-4 ▼]                 │  ← Chat Input Area (35%)
│                                 │
│  [输入问题，按回车发送...] [发送] │
│                                 │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │ 您好！我是A股市场分析    │   │  ← AI Message
│  │ 助手。请输入您想了解...  │   │
│  └─────────────────────────┘   │
│                                 │
│              ┌─────────────┐   │
│              │ 分析一下AI  │   │  ← User Message
│              │ 相关热搜    │   │
│              └─────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │  ← Results Area (65%)
│  │ 基于当前热搜数据分析...  │   │     (Scrollable)
│  │                         │   │
│  │ 1. 市场情绪指标...      │   │
│  │ 2. 技术面关键...        │   │
│  └─────────────────────────┘   │
│                                 │
│  [···] 正在输入...              │  ← Typing Indicator
│                                 │
└─────────────────────────────────┘
```

**数据分析板块整体布局：**

```
┌────────────────────────────────────────────────────────────┐
│  [🔴] 今日热搜                                10:30 更新   │
├────────────────────────────────────────────────────────────┤
│  [平台热搜] [数据分析] ← active                              │
├────────────────────────────────────────────────────────────┤
│  [+ 添加分析主题]                                          │
│                                                            │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│   │ A股市场分析 │  │ AI技术前沿  │  │ 国际局势    │       │  ← Analysis Cards
│   │ GPT-4      │  │ Claude 3    │  │ GPT-4       │       │
│   │ [聊天区]   │  │ [聊天区]    │  │ [聊天区]    │       │
│   │ [展示区]   │  │ [展示区]    │  │ [展示区]    │       │
│   └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

#### 4.2.2 对话流程

1. **用户输入**：在输入框中输入分析主题或问题
2. **数据源准备**：系统自动加载当前缓存的150条热搜数据
3. **筛选处理**（可选）：根据用户选择的平台或关键词筛选数据
4. **Prompt构建**：组装系统Prompt + 热搜数据 + 用户问题
5. **流式响应**：大模型返回流式输出，逐字显示在对话框
6. **上下文保持**：支持多轮对话，保持上下文连贯性

#### 4.2.3 大模型选型

**优先方案：智谱AI GLM-4-Flash**

| 指标 | 限制 | 评估 |
|-----|------|------|
| RPM | 100 | 足够支撑20并发用户 |
| TPM | 100K | 足够 |
| 费用 | 免费 | 适合个人项目 |
| 流式输出 | 支持 | 用户体验好 |

**备选方案**：讯飞星火Lite、阿里通义千问-Turbo

**降级方案**：当API限流时，提示用户"当前分析人数较多，请稍后重试"

#### 4.2.4 Prompt设计

```
你是一位数据分析助手。基于以下今日热搜数据，回答用户的问题。

【热搜数据】
{platform}平台共{count}条：
{formatted_data}

【用户问题】
{user_question}

请提供简洁、有洞察力的分析。
```

### 4.3 API接口设计

#### 4.3.1 获取热搜数据

```
GET /api/hotsearch/:platform

Path参数：
  platform: zhihu | bilibili | weibo

响应：
{
  "code": 0,
  "data": {
    "platform": "zhihu",
    "updateTime": "2026-05-28T10:30:00Z",
    "items": [
      {
        "rank": 1,
        "title": "热榜标题",
        "url": "https://...",
        "heat": 1234567
      }
    ]
  }
}

错误响应：
{
  "code": -1,
  "message": "获取失败，使用缓存数据",
  "data": { /* 缓存数据 */ }
}
```

#### 4.3.2 获取全部平台数据

```
GET /api/hotsearch

响应：
{
  "code": 0,
  "data": {
    "zhihu": { /* 知乎数据 */ },
    "bilibili": { /* B站数据 */ },
    "weibo": { /* 微博数据 */ }
  }
}
```

#### 4.3.3 AI分析对话

```
POST /api/analyze

请求体：
{
  "message": "分析一下AI相关热搜",
  "platform": "all",  // all | zhihu | bilibili | weibo
  "keyword": "AI"     // 可选，关键词筛选
}

响应：SSE流式输出
```

---

## 5. 技术架构（概述）

> 详细技术设计请参阅 **TECH_DESIGN.md**

### 5.1 技术栈选型

| 层级 | 技术选型 | 说明 |
|-----|---------|------|
| **前端** | React 18 + TypeScript 5 + Vite 5 | 响应式UI，组件化开发 |
| **后端** | Node.js 18 LTS + Express 4 | API服务、数据抓取、缓存管理 |
| **数据库** | SQLite / PostgreSQL | 本地SQLite，云端PostgreSQL |
| **缓存** | 内存 Map / Redis | 10分钟TTL，降级方案 |
| **部署** | Vercel + Railway | 前端Vercel CDN，后端Railway容器 |

### 5.2 系统架构示意

```
用户浏览器 → Vercel CDN(前端) → Railway(后端) → 各平台API
                                    ↓
                              SQLite/PostgreSQL
```

### 5.3 数据流说明

1. **定时抓取**：后端每10分钟抓取各平台热搜
2. **缓存策略**：内存缓存，TTL 600秒，失败降级
3. **API响应**：优先读缓存，未命中则实时抓取
4. **数据统一**：不同平台格式统一为 HotItem 标准结构

---

## 6. 界面设计要求

### 6.1 设计原则

- **现代卡片式**：采用圆角卡片+阴影的设计风格，层次清晰
- **平台品牌色**：每个平台使用其品牌主色作为视觉识别（知乎蓝、B站粉、微博红）
- **信息密度适中**：标题+热度+标签，单屏可预览全部内容
- **响应式优先**：桌面三列、平板两列、手机单列自适应
- **微交互反馈**：悬停高亮、点击跳转、加载动画等细节体验

### 6.2 配色方案（CSS Variables）

**基础色系：**

| 变量名 | 色值 | 用途 |
|-------|------|------|
| `--bg` | #f4f5f7 | 页面主背景 |
| `--surface` | #ffffff | 卡片背景 |
| `--border` | #e8eaed | 边框、分隔线 |
| `--text-primary` | #1a1a1a | 主标题、正文 |
| `--text-secondary` | #6b7280 | 次要文字 |
| `--text-muted` | #9ca3af | 辅助文字、时间戳 |

**平台品牌色：**

| 平台 | 变量名 | 色值 | 用途 |
|-----|-------|------|------|
| 知乎 | `--accent-zhihu` | #0084ff | 图标、徽章、悬停 |
| B站 | `--accent-bilibili` | #fb7299 | 图标、徽章、悬停 |
| 微博 | `--accent-weibo` | #e6162d | 图标、徽章、悬停 |

**排名标识色：**

| 排名 | 色值 | 说明 |
|-----|------|------|
| Top 1 | #e6162d | 红色，最醒目 |
| Top 2 | #f97316 | 橙色 |
| Top 3 | #eab308 | 黄色 |
| 其他 | #9ca3af | 灰色 |

**UI元素色：**

| 元素 | 色值 | 说明 |
|-----|------|------|
| 主按钮 | #1d6bf6 | 发送、确认按钮 |
| 悬停背景 | #f9fafb | 列表项悬停 |
| 用户消息背景 | #e8f4fd | 聊天界面用户气泡 |
| AI消息背景 | #ffffff | 聊天界面AI气泡 |

### 6.3 响应式断点

| 设备 | 宽度 | 布局 | 间距 |
|-----|------|------|------|
| 桌面 | ≥1200px | 3列并排 | 16px |
| 平板 | 768px-1199px | 2列 | 16px |
| 手机 | <560px | 单列堆叠 | 12px |

**页面结构：**
- 最大宽度：1200px，居中
- 页面内边距：20px（桌面）、12px（手机）
- 卡片间距：16px（Grid gap）

### 6.4 组件规范

**顶部导航栏（Header）：**
- 高度：56px
- 背景：白色 + 底部边框（1px solid #e8eaed）
- 固定定位：sticky top
- 左侧：站点名称「今日热搜」（标题20px加粗，「热搜」用红色 #e6162d）
- 右侧：当前时间（12px灰色）

**标签页（Tabs）：**
- 背景：白色
- 底部边框：1px solid #e8eaed
- Tab按钮：内边距14px 24px，字体15px
- 激活态：文字黑色 + 底部2.5px红色指示条
- 未激活：文字灰色 #9ca3af

**热搜卡片：**
- 圆角：10px
- 阴影：`0 1px 3px rgba(0,0,0,.08), 0 1px 2px rgba(0,0,0,.04)`
- 悬停阴影：`0 4px 12px rgba(0,0,0,.1)`
- 过渡动画：box-shadow 0.2s

**卡片头部（Card Header）：**
- 内边距：14px 16px 12px
- 底部边框：1px solid #e8eaed
- 平台图标：28px × 28px，圆角6px，白色文字
- 平台名称：15px 加粗
- 徽章（Badge）：右上角小标签，圆角20px

**列表项（List Item）：**
- 内边距：7px 16px
- 排名：20px宽度，居中，14px字体
- 标题：13.5px，单行截断（ellipsis）
- 热度：11px，灰色
- 标签：10px小字，带背景色（如AI紫色、金融橙色）
- 悬停：背景色 #f9fafb，标题变红色

**卡片底部（Card Footer）：**
- 内边距：8px 16px 10px
- 顶部边框：1px solid #e8eaed
- 文字：11px灰色，显示更新时间

**数据分析卡片（Analysis Card）：**
- 固定高度：520px
- 分区布局：
  - 上部（35%）：聊天输入区，背景 #f7f8fa
  - 下部（65%）：对话展示区，可滚动

**分析卡片头部：**
- 图标：32px × 32px，圆角8px，渐变色背景
- 标题：14px 加粗
- 删除按钮：右上角 × 号，悬停变红

**聊天输入区：**
- 模型选择：下拉框，12px字体
- 输入框：圆角6px，边框 #e8eaed，聚焦变蓝
- 发送按钮：蓝色背景 #1d6bf6，白色文字

**消息气泡：**
- 用户：蓝色背景 #e8f4fd，右对齐，底部圆角4px
- AI：白色背景，带边框，左对齐，底部圆角4px
- 内边距：8px 12px，字体13px

---

## 7. 非功能性需求

### 7.1 性能指标

**测试条件**：本地局域网、Chrome浏览器、桌面端、100Mbps带宽

| 指标 | 要求 | 测试方法 |
|-----|------|---------|
| 页面首屏加载时间 | ≤ 3秒 | Lighthouse性能检测，禁用缓存后首次访问 |
| 平台热搜查询响应 | ≤ 1秒 | Network面板，命中缓存情况下 |
| 大模型首字响应 | ≤ 5秒 | 从点击发送到大模型返回第一个字符 |
| 并发支持 | 20人同时在线 | 工具模拟20用户同时访问 |

### 7.2 可用性指标

| 指标 | 要求 |
|-----|------|
| 系统可用性 | ≥ 95%（允许日常维护停机） |
| 数据更新延迟 | 平台热搜 ≤ 10分钟 |
| 故障恢复 | 服务重启后缓存数据自动恢复 |

### 7.3 安全与合规

| 类别 | 措施 |
|-----|------|
| 内容过滤 | 建立违规词库，命中关键词的内容不展示 |
| API安全 | 接口限流，异常访问检测 |
| 日志安全 | 不记录用户搜索关键词等敏感信息 |
| 防爬虫 | robots.txt禁止搜索引擎收录 |
| 访问限速 | 单用户每分钟访问次数限制 |

### 7.4 可维护性

| 指标 | 要求 |
|-----|------|
| 日志保留 | 应用日志保留7天 |
| 监控告警 | 核心接口错误率超过10%时触发告警 |
| 数据备份 | 数据库每周自动备份 |

---

## 8. 部署方案（概述）

> 详细部署配置请参阅 **TECH_DESIGN.md** 第8章

### 8.1 方案对比

| 方案 | 成本 | 适用场景 | 技术栈 |
|-----|------|---------|--------|
| **Vercel + Railway** | ~$5/月 | 正式对外、学习项目 | 前端CDN + 容器后端 |
| **腾讯云单服务器** | ~80元/月 | 国内优化、完全可控 | Nginx + PM2 |
| **Windows本地** | 0元 | 开发测试、局域网 | Node.js直连 |

### 8.2 推荐方案：Vercel + Railway

**架构设计**：
- **前端**：Vercel（全球CDN、自动HTTPS）
- **后端**：Railway（容器化、自动扩缩容）
- **数据库**：SQLite / PostgreSQL
- **成本**：约$5/月（35元）

**部署流程**：
1. 前端推送到 GitHub → Vercel自动部署
2. 后端推送到 GitHub → Railway容器化部署
3. 配置环境变量、CORS跨域
4. 绑定自定义域名（可选）

### 8.3 本地开发部署

```bash
# 1. 启动后端
cd server
npm install
npm run dev          # http://localhost:3000

# 2. 启动前端
cd client
npm install
npm run dev          # http://localhost:5173
```

---


---

## 9. 附录

### 9.1 文档关联

| 文档 | 说明 |
|-----|------|
| **RESEARCH.md** | 需求研究文档，包含用户场景、竞品分析 |
| **TECH_DESIGN.md** | 技术设计文档，包含详细架构、API、部署配置 |

### 6.5 移动端适配细节

#### 6.5.1 触摸交互优化

**点击区域：**
- 所有可点击元素最小高度：44px（符合苹果HIG规范）
- 列表项内边距：桌面7px 16px → 手机12px 16px
- 按钮最小尺寸：44px × 44px

**悬停效果处理：**
```css
/* 桌面端：悬停效果 */
@media (hover: hover) {
  .list-item:hover {
    background: var(--hover-bg);
  }
  .list-item:hover .item-title {
    color: var(--accent-red);
  }
}

/* 移动端：点击效果替代悬停 */
@media (hover: none) {
  .list-item:hover {
    background: transparent;
  }
  .list-item:active {
    background: var(--hover-bg);
  }
}
```

#### 6.5.2 字体大小适配

| 元素 | 桌面端 | 手机端 | 说明 |
|-----|--------|--------|------|
| 站点标题 | 20px | 18px | 保持品牌识别 |
| 平台名称 | 15px | 14px | 卡片头部 |
| 热搜标题 | 13.5px | 14px | 手机端适当增大 |
| 热度数值 | 11px | 12px | 保证可读性 |
| 更新时间 | 11px | 11px | 保持不变 |
| 标签文字 | 10px | 11px | 适当增大 |

**字体渲染优化：**
```css
html {
  -webkit-text-size-adjust: 100%;  /* 禁止iPhone自动调整字体 */
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
```

#### 6.5.3 底部安全区适配（刘海屏）

```css
/* iPhone X+ 底部安全区 */
body {
  padding-bottom: env(safe-area-inset-bottom, 0);
}

/* 固定底部元素适配 */
.fixed-bottom {
  padding-bottom: env(safe-area-inset-bottom, 0);
  padding-bottom: constant(safe-area-inset-bottom);  /* iOS 11.0 */
}
```

**viewport配置：**
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
```

#### 6.5.4 数据分析卡片手机适配

**布局调整：**
- 桌面固定高度520px → 手机自适应高度或减小到400px
- 桌面双列卡片网格 → 手机单列全宽

**交互优化：**
- 输入框固定在屏幕底部（避免键盘遮挡）
- 软键盘弹出时，对话区域自动滚动到底部
- 添加"发送"按钮放大到44px以上

```css
/* 手机端分析卡片 */
@media (max-width: 560px) {
  .analysis-card {
    height: auto;
    min-height: 400px;
    max-height: calc(100vh - 200px);
  }
  
  .analysis-chat {
    position: sticky;
    bottom: 0;
    background: var(--chat-bg);
    border-top: 1px solid var(--border);
  }
  
  .chat-send {
    min-width: 60px;
    height: 44px;
  }
}
```

#### 6.5.5 横向滚动处理

**热搜卡片内容溢出：**
```css
/* 防止横向滚动 */
body {
  overflow-x: hidden;
}

/* 长标题截断 */
.item-title {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: calc(100vw - 120px);  /* 手机端动态计算 */
}
```

#### 6.5.6 手势支持

| 手势 | 功能 | 触发区域 |
|-----|------|---------|
| 下拉刷新 | 刷新热搜数据 | 平台热搜标签页 |
| 左右滑动 | 切换标签页 | 整个页面 |
| 长按 | 复制热搜标题 | 热搜列表项 |

