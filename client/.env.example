# 开发环境：vite 代理会自动处理 /api 请求
# 无需配置 VITE_API_BASE_URL，或设置为 /api

# 生产环境：设置后端 API 地址
# 例如 Vercel 部署时指向 Railway 后端
VITE_API_BASE_URL=https://your-railway-app.up.railway.app

# 本地开发时如需直接访问后端（跳过代理），可设置为：
# VITE_API_BASE_URL=http://localhost:3001
